// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/mandacode-labs/mdrive/ent/drive"
	"github.com/mandacode-labs/mdrive/ent/superblock"
)

// Superblock is the model entity for the Superblock schema.
type Superblock struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// RootNodeID holds the value of the "root_node_id" field.
	RootNodeID uuid.UUID `json:"root_node_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SuperblockQuery when eager-loading is set.
	Edges            SuperblockEdges `json:"edges"`
	drive_superblock *string
	selectValues     sql.SelectValues
}

// SuperblockEdges holds the relations/edges for other nodes in the graph.
type SuperblockEdges struct {
	// Drive holds the value of the drive edge.
	Drive *Drive `json:"drive,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DriveOrErr returns the Drive value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SuperblockEdges) DriveOrErr() (*Drive, error) {
	if e.Drive != nil {
		return e.Drive, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: drive.Label}
	}
	return nil, &NotLoadedError{edge: "drive"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Superblock) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case superblock.FieldCreateTime, superblock.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case superblock.FieldID, superblock.FieldRootNodeID:
			values[i] = new(uuid.UUID)
		case superblock.ForeignKeys[0]: // drive_superblock
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Superblock fields.
func (_m *Superblock) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case superblock.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case superblock.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				_m.CreateTime = value.Time
			}
		case superblock.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				_m.UpdateTime = value.Time
			}
		case superblock.FieldRootNodeID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field root_node_id", values[i])
			} else if value != nil {
				_m.RootNodeID = *value
			}
		case superblock.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field drive_superblock", values[i])
			} else if value.Valid {
				_m.drive_superblock = new(string)
				*_m.drive_superblock = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Superblock.
// This includes values selected through modifiers, order, etc.
func (_m *Superblock) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryDrive queries the "drive" edge of the Superblock entity.
func (_m *Superblock) QueryDrive() *DriveQuery {
	return NewSuperblockClient(_m.config).QueryDrive(_m)
}

// Update returns a builder for updating this Superblock.
// Note that you need to call Superblock.Unwrap() before calling this method if this Superblock
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Superblock) Update() *SuperblockUpdateOne {
	return NewSuperblockClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Superblock entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Superblock) Unwrap() *Superblock {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Superblock is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Superblock) String() string {
	var builder strings.Builder
	builder.WriteString("Superblock(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("create_time=")
	builder.WriteString(_m.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(_m.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("root_node_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RootNodeID))
	builder.WriteByte(')')
	return builder.String()
}

// Superblocks is a parsable slice of Superblock.
type Superblocks []*Superblock

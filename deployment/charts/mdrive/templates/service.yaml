apiVersion: v1
kind: Service
metadata:
  name: {{ include "mdrive.fullname" . }}
  labels:
    {{- include "mdrive.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mdrive.selectorLabels" . | nindent 4 }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mdrive.fullname" . }}
  labels:
    {{- include "mdrive.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "mdrive.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secrets: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "mdrive.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "mdrive.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automount }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ include "mdrive.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /app/mdrive
          args:
            - api-server
            - run
            - --config
            - /app/config/config.yaml
          env:
            {{- include "mdrive.secretEnv" (dict "envName" "DATABASE_PASSWORD" "existingSecret" .Values.secrets.database.existingSecret "secretKey" (.Values.secrets.database.existingSecretKey | default "database-password") "root" .) | nindent 12 }}
            - name: VALKEY_USER
              value: {{ .Values.config.valkey.user | default "default" | quote }}
            {{- if or .Values.secrets.valkey.password .Values.secrets.valkey.existingSecret }}
            {{- include "mdrive.secretEnv" (dict "envName" "VALKEY_PASSWORD" "existingSecret" .Values.secrets.valkey.existingSecret "secretKey" (.Values.secrets.valkey.existingSecretKey | default "valkey-password") "root" .) | nindent 12 }}
            {{- end }}
            {{- if or .Values.secrets.storage.access_key .Values.secrets.storage.existingSecret }}
            {{- include "mdrive.secretEnv" (dict "envName" "STORAGE_ACCESS_KEY" "existingSecret" .Values.secrets.storage.existingSecret "secretKey" (.Values.secrets.storage.existingSecretKeyAccess | default "storage-access-key") "root" .) | nindent 12 }}
            {{- include "mdrive.secretEnv" (dict "envName" "STORAGE_SECRET_KEY" "existingSecret" .Values.secrets.storage.existingSecret "secretKey" (.Values.secrets.storage.existingSecretKeySecret | default "storage-secret-key") "root" .) | nindent 12 }}
            {{- end }}
            {{- if or .Values.secrets.crypto.master_key .Values.secrets.crypto.existingSecret }}
            {{- include "mdrive.secretEnv" (dict "envName" "CRYPTO_MASTER_KEY" "existingSecret" .Values.secrets.crypto.existingSecret "secretKey" (.Values.secrets.crypto.existingSecretKey | default "crypto-master-key") "root" .) | nindent 12 }}
            {{- end }}
            {{- if and .Values.config.auth.encryption_key (ne .Values.config.auth.encryption_key "") }}
            - name: AUTH_ENCRYPTION_KEY
              value: {{ .Values.config.auth.encryption_key | quote }}
            {{- else if or .Values.secrets.auth.encryption_key .Values.secrets.auth.existingSecret }}
            {{- include "mdrive.secretEnv" (dict "envName" "AUTH_ENCRYPTION_KEY" "existingSecret" .Values.secrets.auth.existingSecret "secretKey" (.Values.secrets.auth.existingSecretKey | default "auth-encryption-key") "root" .) | nindent 12 }}
            {{- end }}
            {{- if or .Values.secrets.openfga.api_token .Values.secrets.openfga.existingSecret }}
            {{- include "mdrive.secretEnv" (dict "envName" "OPENFGA_API_TOKEN" "existingSecret" .Values.secrets.openfga.existingSecret "secretKey" (.Values.secrets.openfga.existingSecretKeyApiToken | default "openfga-api-token") "root" .) | nindent 12 }}
            {{- end }}
            {{- if or .Values.secrets.openfga.client_id .Values.secrets.openfga.existingSecret }}
            {{- include "mdrive.secretEnv" (dict "envName" "OPENFGA_CLIENT_ID" "existingSecret" .Values.secrets.openfga.existingSecret "secretKey" (.Values.secrets.openfga.existingSecretKeyClientID | default "openfga-client-id") "root" .) | nindent 12 }}
            {{- end }}
            {{- if or .Values.secrets.openfga.client_secret .Values.secrets.openfga.existingSecret }}
            {{- include "mdrive.secretEnv" (dict "envName" "OPENFGA_CLIENT_SECRET" "existingSecret" .Values.secrets.openfga.existingSecret "secretKey" (.Values.secrets.openfga.existingSecretKeyClientSecret | default "openfga-client-secret") "root" .) | nindent 12 }}
            {{- end }}
            - name: OPENFGA_SCOPES
              value: {{ .Values.config.openfga.scopes | quote }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.config.http.port }}
              protocol: TCP
          {{- with .Values.startupProbe }}
          startupProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /app/config
              readOnly: true
            - name: tmp
              mountPath: /tmp
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "mdrive.fullname" . }}
        - name: tmp
          emptyDir: {}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mdrive.fullname" . }}
  labels:
    {{- include "mdrive.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ .Values.config | toYaml | nindent 4 }}

package driveop

import (
	"context"

	"github.com/mandacode-labs/mdrive/internal/vfs"
)

// InitializeDrive implements [vfs.DriveOperation].
func (d *driveOperation) InitializeDrive(ctx context.Context, drive *vfs.Drive) error {
	panic("unimplemented")
}

Error:
  type: object
  required: [code, message]
  properties:
    code:
      type: string
      description: Machine-readable error code
      enum: [not_found, conflict, bad_request, forbidden, unauthorized, internal, revision_conflict]
    message:
      type: string
      description: Human-readable error message
